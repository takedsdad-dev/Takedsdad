# تاكد سداد

موقع عربي لخدمات تسديد القروض والمتعثرات، مبني بـ Vite و React و TypeScript.

رقم التواصل موجود في `src/site.ts`. غيّر `phone` و `whatsapp` قبل النشر.

## التشغيل

- `npm install`
- `npm run dev`
- `npm run build`
